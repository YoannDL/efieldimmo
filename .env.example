PORT=3000
DB_PATH=./data/efield-immo.sqlite
SESSION_SECRET=change-me-to-a-long-random-string
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me-too

# Optional: email notification on each new inquiry.
# Leave empty to disable (inquiries are always stored in the admin panel).
# For Gmail: SMTP_HOST=smtp.gmail.com, SMTP_PORT=587, SMTP_USER=you@gmail.com,
# SMTP_PASS=an "App password" (myaccount.google.com/apppasswords), NOTIFY_EMAIL=you@gmail.com
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
NOTIFY_EMAIL=
